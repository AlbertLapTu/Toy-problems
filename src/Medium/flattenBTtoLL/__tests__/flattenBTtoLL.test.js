const fn = require('../flattenBTtoLL');

describe('Medium - Flatten BT to LinkedList', () => {
  test('It should exist', () => {
    expect(fn).toBeTruthy();
  });

  test.skip('Should flatten a binary tree to a linkedList', () => {});
});
