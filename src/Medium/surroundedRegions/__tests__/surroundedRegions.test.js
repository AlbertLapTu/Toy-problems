const fn = require('../surroundedRegions');

describe('Medium - Surrounded Regions', () => {
  test('It should exist', () => {
    expect(fn).toBeTruthy();
  });

  // Ignoring test to save memory and faster tests. My solution does not work.
});
